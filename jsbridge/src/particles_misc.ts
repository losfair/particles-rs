export interface ParticlesConfig {
    height: number,
    width: number,
    nParticles: number,
    maxEdgeLen: number,
    velocityFactor: number,
    nodeColor: string,
    nodeRadius: number,
    lineColor: string,
    lineWidth: number
}
